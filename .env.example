# Copy this file to .env and fill in real values.

# Required for src/discovery/verify.py and src/governance/extractor.py
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Hugging Face token, only needed if the dataset requires auth
HUGGINGFACE_TOKEN=

# Local storage paths (defaults shown)
DATA_DIR=./data
SQLITE_DB_PATH=./data/processed/evidence.db
